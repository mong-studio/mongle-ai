## Storage (S3 사용 시)
AWS_REGION=<REGION>
AWS_S3_BUCKET=<BUCKET>
AWS_S3_PREFIX=mongle-village

## OpenAI (이미지 생성 / VLM 등에 여전히 필요)
OPENAI_API_KEY=<OPENAI_API_KEY>

# Ollama 사용 시:
#   $ ollama pull qwen2.5:7b
#   $ ollama serve
QWEN_BASE_URL=http://localhost:11434/v1
QWEN_MODEL=qwen2.5:7b
QWEN_API_KEY=EMPTY

# 또는 vLLM:
#   $ vllm serve Qwen/Qwen2.5-7B-Instruct --port 8000
#   QWEN_BASE_URL=http://localhost:8000/v1
#   QWEN_MODEL=Qwen/Qwen2.5-7B-Instruct

# 생성 이미지 저장 위치(기본: 로컬 저장)
STORAGE_BACKEND=local
LOCAL_STORAGE_ROOT=

# 캐릭터 이미지 생성용 LoRA 가중치 폴더
LORA_DIR=

# 퀘스트 생성은 기본적으로 Qwen을 사용합니다.
# 외부 모델 호출 없이 화면만 확인하려면 fake로 바꾸세요.
QUEST_LLM_PROVIDER=qwen

# 디버그 로그를 끄려면 0으로 바꾸세요.
MONGLE_DEBUG_TODO=1
MONGLE_DEBUG_CHARACTER=1


