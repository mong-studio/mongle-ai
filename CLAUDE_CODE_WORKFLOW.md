# Claude Code로 게임/시각 프로젝트 작업하기

> **이 문서는 실제 작업 후기로 쓰였습니다.** 우리는 16시간 동안 픽셀
> 마을 게임을 만들었고, 그 절반은 **피할 수 있었던 시행착오**였습니다.
> 학생 여러분이 같은 함정에 빠지지 않도록 패턴을 정리합니다.

---

## 0. 무엇이 잘못됐는가 (정직한 회고)

이번 프로젝트에서 우리가 반복한 실수 5개:

| #   | 실수                                                      | 비용                                                                | 어떻게 발견됨                               |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------- |
| 1   | 집 크기를 5×3 타일로 가정했는데 실제는 4×3                | 모든 집이 옆집 일부를 포함해서 "미완성" 같이 보임. **3번의 재빌드** | 사용자가 "집이 깨져 보인다"고 지적          |
| 2   | 잔디 타일 (12,16)에 1픽셀 검은 외곽선이 있는 걸 못 봄     | 마을 전체에 **수직 줄무늬** 생김                                    | 사용자가 "줄이 보인다" 지적 → 그제서야 진단 |
| 3   | "부쉬"라고 등록한 (12,11)이 사실 **나무 그루터기**        | 마을 사방에 그루터기 박혀있음                                       | 사용자가 "이상한 게 있다" 지적              |
| 4   | "꽃"이라고 한 (2,9)가 사실 **당근 아이콘**                | 마을에 당근이 떨어져 있음                                           | 같은 라운드에 발견                          |
| 5   | 사용자의 레퍼런스가 AI 페인팅이라는 걸 4라운드째에야 인지 | 16×16 픽셀로 페인팅 따라하려고 함                                   | 솔직한 한계 인정 후 방향 전환               |

**공통 원인:** 작은 단위로 검증하지 않고 큰 단위로 만들어서 한 번에 확인.

---

## 1. 시작 전 — 5분 체크리스트

코드 한 줄 짜기 전에:

### A. 목표 이미지 분류

사용자가 레퍼런스 이미지를 주면, **그게 뭔지 분류부터**:

- [ ] **A형: 실제 게임 스크린샷** → 비슷한 에셋팩 찾으면 90% 가능
- [ ] **B형: AI 페인팅 / 컨셉아트** → 픽셀게임으로는 **물리적으로 불가능**. 솔직히 말하고 다음 행동 합의.
- [ ] **C형: 직접 그린 일러스트** → 픽셀 변환 어렵지만 색감/구도는 참고 가능

> 💡 분간이 안 되면 Claude한테 "이게 실제 게임 스샷 같아 아니면 AI/페인팅 같아?"
> 라고 물어보세요. 솔직히 답해줍니다.

### B. 에셋 먼저 확정

**코드 작성 전에 에셋을 결정합니다.**

```
□ 어느 팩을 쓸 것인가? (Sparklin? Kenney? 유료 Sprout Lands?)
□ 라이선스는 무엇인가? (CC0인지 확인)
□ 타일 사이즈가 게임 기조에 맞는가? (16×16 chibi vs 32×32 standard)
□ 에셋팩의 미술 스타일이 원하는 것과 같은가?
```

### C. 검증 인프라 먼저

**스크린샷 한 장 뽑는 데 5분 걸리면 못 만듭니다.**

이번 프로젝트의 결정적 도구는 처음에 만들었어야 했던 것들:

```bash
# 1) 헤드리스 실행 + 스크린샷
xvfb-run godot --headless --resolution 1920x1080 tools/screenshot.tscn

# 2) 타일셋 검사 그리드 (각 타일에 좌표 라벨)
python3 tools/inspect_tileset.py  # 우리는 5라운드 뒤에야 만듦

# 3) 신뢰 가능한 단일 타일 미리보기
python3 -c "from PIL import Image; ..."  # 각 좌표가 진짜 그건지 확인
```

이 3개를 **첫 30분 안에** 만드세요. 안 그러면 매 변경마다 30분씩 날립니다.

---

## 2. Claude에게 일 시키는 법

### 잘 시키는 법

```
✅ 좋은 프롬프트
"이 타일셋(첨부)에서 잔디 타일 좌표 찾아줘.
조건: 단색 녹색, 외곽선 없음, 16×16 타일.
후보 3개 만들고 각각 단독으로 보여줘. 결정은 내가 함."
```

```
❌ 나쁜 프롬프트
"잔디 어디야"
```

### Claude의 한계 인정시키기

게임/시각 작업에서 Claude는 이런 거 못합니다 (적어도 잘 못함):

- **이미지를 픽셀 단위로 정확히 읽기** — 비주얼 매칭은 사람 눈이 더 정확
- **에셋 좌표 추측** — 항상 후보 보여달라고 하세요
- **AI 페인팅 스타일 재현** — 픽셀로는 불가
- **"이쁘게 만들어줘"** — 기준이 없으면 짐작함 → 실수

### Claude를 잘 쓰는 패턴

1. **3개 후보 강제**: "후보 3개 만들고 보여줘" (즉답하면 추측이 많음)
2. **진단 먼저, 수정 뒤**: "왜 이렇게 보이는지 진단부터 한 뒤 수정안 제시"
3. **솔직함 요청**: "이게 정말 우리 에셋으로 가능해?" — 자주 "불가능"이 답
4. **작은 단위 빌드**: "전체 마을 말고 30×20 타일 영역만 먼저"

---

## 3. 빌드 → 검증 → 수정 루프 (1분 단위)

게임/시각 작업의 황금률: **루프를 1분 안에 끝낼 수 있어야** 한다.

```
변경        ← 코드/에셋 수정
  ↓
빌드        ← python3 build.py  (10초 안에 끝나야)
  ↓
캡처        ← godot --quit-after 60 + screenshot  (15초)
  ↓
보기        ← Read 또는 SendUserFile로 확인
  ↓
판정        ← 이상하면 진단, 좋으면 커밋
```

이번 프로젝트에서 우리는 **그렇게 했습니다 (v8 이후)**. 그 전에는 한 라운드에 5분씩 걸렸어요.

### 커밋 = 세이브 포인트

좋은 상태 = 즉시 커밋. 망가지면 `git reset --hard HEAD`로 1초 복귀.

```bash
git commit -m "WIP: 잔디 줄무늬 사라짐 (14,16) 사용"
# 다음 시도하다가 망하면 → git reset --hard HEAD
```

---

## 4. 사용할 만한 Claude Code 도구들

### 슬래시 명령어 (`/`)

| 명령           | 언제                                  |
| -------------- | ------------------------------------- |
| `/run`         | 변경 후 실제로 앱 돌려서 확인         |
| `/verify`      | "이 수정이 진짜 작동해?" 명시적 검증  |
| `/code-review` | 변경 분량 큰 PR 전 자기검토           |
| `/init`        | CLAUDE.md 자동 생성 (프로젝트 컨벤션) |
| `/ultrareview` | 큰 마일스톤마다 멀티-에이전트 리뷰    |

### 에이전트 분리 (서브에이전트)

긴 탐색은 메인 컨텍스트 더럽힘. Explore 서브에이전트로 보내세요:

```
"이 타일셋에서 잔디·길·집 좌표를 모두 찾아서 리포트해줘"
→ Agent (Explore) 서브 에이전트로 위임
→ 결과만 메인에 돌아옴
```

### MCP 서버

- **GitHub MCP**: PR/이슈 처리 — `mcp__github__*` 툴
- **Figma MCP**: 디자인 → 코드 (이번 프로젝트와는 무관했지만)

### 백그라운드 실행

빌드/테스트는 백그라운드로:

```
Bash(run_in_background=true)
→ 다른 일 하면서 완료 알림 기다림
```

---

## 5. 안티패턴 — 우리가 한 실수들

### ❌ "보이는 대로 좌표 찍기"

```python
# 우리가 한 짓
BUSH = tile(12, 11)  # 시각으로 봤을 때 부쉬 같아 보임 → 사실 그루터기
```

✅ **개별 미리보기 강제**

```python
# 변경 전 항상
Image.open(...).crop(...).save("/tmp/preview_12_11.png")
# 사용자/Claude에게 보여서 "이게 그거 맞아?" 확인
```

### ❌ "큰 단위로 만들고 한 번에 보기"

```
마을 전체 빌드 → 게임 실행 → 깨져 보임 → 어디가 문제?
```

✅ **단일 객체 빌드부터**

```
1) 잔디 4×4 만 깔아본다 → 줄무늬 있나?
2) 집 한 채만 놓아본다 → 완성형이가?
3) 둘 합쳐본다 → OK
4) 풀 마을로 확장
```

### ❌ "사용자 레퍼런스를 무조건 따라잡으려고"

사용자가 AI 페인팅 레퍼런스를 줬는데 **첫 라운드에 분류 안 함** → 4라운드 동안 16×16으로 페인팅 흉내내려고 헛수고.

✅ **레퍼런스 분류 + 합의**

```
"이 이미지는 AI 페인팅인 것 같은데, 픽셀게임으로 100% 같게는 못 만들어요.
방향 합의:
 (A) 스타듀밸리 수준이면 OK
 (B) AI 이미지 그대로 배경으로 깔기
 (C) 유료 32×32 에셋 사기"
```

### ❌ "음수 피드백 무시"

사용자: "뭔가 깨져 보인다" → "어디가?" 다시 묻지 말고 **이미지 보내 달라고** 하고 픽셀 단위로 진단.

✅ **항상 시각 증거**

```
□ 깨져 보인다는데 어디가? → 스크린샷 + 빨간 동그라미
□ "이상하다"의 진짜 원인? → 후보 3개 진단 후 픽스
```

---

## 6. 복사해서 쓰는 프롬프트 템플릿

### 시작할 때

```
이 프로젝트 [목표] 만들거야. 레퍼런스 [첨부].

먼저 이거 분류해줘:
1) 실제 게임 스샷인가, AI 페인팅인가?
2) 우리가 쓸 수 있는 무료 CC0 에셋 중 가장 가까운 거 3개?
3) 솔직히 어느 수준까지 가능한지 (95% / 70% / 30%)?

답 보고 방향 정할게.
```

### 새 기능 추가할 때

```
[X 기능] 추가하고 싶어.

조건:
- 기존 [Y, Z] 깨지면 안 됨
- 빌드 시간 1분 이내
- 검증 방법: [구체적 시나리오]

설계안 2가지 줘. 차이점이랑 트레이드오프 명시.
```

### 시각 결과물 검증할 때

```
이 결과(첨부) 어디가 이상한지 진단해줘.

분석 순서:
1) 픽셀 단위로 의심 영역 3곳 짚기
2) 각 영역의 추정 원인
3) 가장 작은 수정 한 가지부터 제시 (한 번에 큰 변경 X)
```

### 막힐 때

```
[현재 상태]. [원하는 상태].

지금 막힌 거 정직하게 말해줘:
- 이거 우리 에셋/도구로 진짜 가능해?
- 가능하면 가장 빠른 길은?
- 불가능하면 차선이 뭐야?
- 내가 모르고 있는 한계가 있어?
```

---

## 7. CLAUDE.md 필수 항목 (프로젝트 루트)

`/init` 명령으로 생성한 뒤 이런 내용 추가:

```markdown
## 이 프로젝트의 시각 기조

- 에셋: Sparklin Ninja Adventure (CC0), 16×16 chibi
- 타깃 룩: Stardew Valley급 cozy farm
- 절대 시도하지 말 것: AI 페인팅 스타일 재현 (불가능)

## 빌드 / 검증 명령
```

python3 tools/build_village.py # 마을 PNG 합성 (~5초)
godot --headless --quit-after 60 ... # 60프레임 실행 검증
xvfb-run godot ... screenshot.tscn # 스크린샷 캡처

```

## 알려진 함정 (반복 금지)
- 집은 4×3 타일 (5×3 아님)
- 잔디는 (14, 16) (12, 16은 검은 외곽선 있음)
- "부쉬" (12, 11)은 사실 그루터기 — (3, 13) 또는 (9, 15) 쓸 것
- "꽃" (2, 9)는 사실 당근

## 좌표 검증 절차
새 타일 좌표 추가 전:
1. 단독 16×16 크롭 → 128×128 확대 저장
2. 시각으로 확인 ("정말 그거 맞나?")
3. 그 다음에야 build 스크립트에 추가
```

---

## 8. 마치며 — 핵심 3개

1. **검증 인프라 먼저**. 스크린샷 1분 안에 못 뽑으면 그날 일 안 끝남.
2. **작은 단위로 검증**. 마을 통째 빌드 전에 타일 한 개부터 확인.
3. **레퍼런스 정직히 분류**. "이게 진짜 우리가 만들 수 있는 것인가?" — Claude에게도 인간에게도.

이 3개만 지켜도 이번 프로젝트의 노가다 70% 사라집니다.

행운을 빕니다 🌱
